version: '3.8'
services:
  bot:
    build: .
    restart: always
    env_file: .env
    volumes:
      - bot_data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot_data:
